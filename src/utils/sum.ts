export function sum(gameId: String, conts: Number) {
    const game = gameId;
    const num: Number = 1;
    if (game === gameId) {
        return Number(conts) + Number(num);
    } 
    return num;
}
